/* Atribuição (=) */
const resultAtribuicao = 60;

/* Atribuição (+=) */
let resultAdicao = 20
// console.log(resultAdicao = resultAdicao + 70);
/* Pra não ter que ficar repetindo a variável ResultAdicao */
resultAdicao += 70

/* Atribuição (-=) */
let resultSubtracao = 20
// console.log(resultSubtracao = resultSubtracao - 10);
/* Pra não ter que ficar repetindo a variável resultSubtracao */
resultSubtracao -= 10

/* Atribuição (/=) */
let resultDivisao = 20
// console.log(resultDivisao = resultDivisao / 10);
/* Pra não ter que ficar repetindo a variável resultDivisao */
resultDivisao /= 10

/* Atribuição (*=) */
let resultMultiplicacao = 20
// console.log(resultMultiplicacao = resultMultiplicacao * 10);
/* Pra não ter que ficar repetindo a variável resultMultiplicacao */
resultMultiplicacao *= 10

/* Atribuição (*=) */
let resultRestoDivisao = 20
// console.log(resultRestoDivisao = resultRestoDivisao * 10);
/* Pra não ter que ficar repetindo a variável resultRestoDivisao */
resultRestoDivisao %= 12


console.log(resultAdicao, resultSubtracao, resultDivisao, resultMultiplicacao, resultRestoDivisao);
